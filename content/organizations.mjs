/**
 * Organizations Mirai has worked with — REAL organizations only, shown with their permission.
 *
 * Add one entry per organization. Never add placeholder or unverified names.
 *   {
 *     name: "Organization name",                 // required — shown as text when no logo file exists
 *     nameAr: "اسم الجهة",                        // optional Arabic display name (falls back to `name`)
 *     logo: "/brand/orgs/example.svg",            // optional — file under public/; omit for a text fallback
 *     websiteUrl: "https://example.org",          // optional — opens in a new tab (rel="noopener noreferrer")
 *     alt: "Example Organization logo",           // optional accessible name (defaults to `${name} logo`)
 *     caseStudyUrl: "",                           // reserved for later — no portfolio system yet
 *     servicesProvided: [],                       // reserved for later
 *     portfolioContent: null                      // reserved for later
 *   }
 *
 * The homepage section (#clients) renders only when this list has at least one entry.
 */
export const organizations = [];
