# Copy to .env on the server (never commit .env). Run with: node --env-file=.env server.mjs
NODE_ENV=production
PORT=3000
SITE_URL=https://miraisolutions.net

# Lead email via Resend (https://resend.com) — verify the sending domain first
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
MIRAI_LEAD_EMAIL=hello@miraisolutions.net
MIRAI_FROM_EMAIL=Mirai Website <hello@miraisolutions.net>

# WhatsApp number, international format, digits only
MIRAI_WHATSAPP=2010XXXXXXXX

# Theme policy: "light" (default — the light look for everyone until they toggle) or "system" (follow the device setting)
MIRAI_THEME=light
